import { Familjen_Grotesk } from "next/font/google";

// Police unique du site, partagée par la page d'accueil et les espaces connectés.
export const rlFont = Familjen_Grotesk({ subsets: ["latin"], weight: ["400", "500", "600", "700"], display: "swap" });
